# Release

Release builds should:

- run the local test suite;
- record the exact verification commands used;
- avoid claiming hardware or RTOS verification unless it was actually run;
- tag the repository only after the local gates pass.
