# Issues

- Use `bug_report.md` for crashes, compiler failures, and portability regressions.
- Use `security_report.md` for security-sensitive reports.
- Include compiler version, platform, exact header path, and a minimal reproducer.
